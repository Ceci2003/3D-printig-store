---
import Layout from '../components/Layout.astro';
import { getEntry } from 'astro:content';

let settings = null;
try {
  settings = (await getEntry('site', 'settings')).data;
} catch (e) {
  // Settings not found, use defaults
}
---

<Layout title={`404 - ${settings?.siteName || '3D Print Studio'}`}>
  <div class="min-h-[60vh] flex items-center justify-center py-12 px-4">
    <div class="max-w-xl mx-auto text-center">
      <!-- Main Heading -->
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-3">
        404 – Страницата не беше намерена
      </h1>

      <!-- Supporting Text -->
      <p class="text-base md:text-lg text-gray-600 mb-6 max-w-md mx-auto">
        Страницата, която търсите, не съществува или е преместена.
      </p>

      <!-- Visual Element: 404 Illustration -->
      <div class="mb-8 flex justify-center">
        <img
          src="/images/404.png"
          alt="404 - Страницата не беше намерена"
          class="h-48 md:h-56 object-contain"
          loading="eager"
        />
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row gap-3 justify-center items-center w-full sm:w-auto">
        <!-- Primary CTA: Home -->
        <a
          href="/"
          class="flex items-center justify-center h-11 min-h-[2.75rem] bg-blue-600 text-white px-6 rounded-lg font-semibold hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition shadow-md hover:shadow-lg w-full sm:w-auto"
        >
          <svg
            class="w-5 h-5 mr-2 flex-shrink-0"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
            />
          </svg>
          Към началната страница
        </a>

        <!-- Secondary CTA: Products -->
        <a
          href="/products"
          class="flex items-center justify-center h-11 min-h-[2.75rem] bg-white text-blue-600 border border-blue-600 px-6 rounded-lg font-semibold hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition w-full sm:w-auto"
        >
          <svg
            class="w-5 h-5 mr-2 flex-shrink-0"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"
            />
          </svg>
          Продукти
        </a>

        <!-- Tertiary CTA: Contact -->
        <a
          href="/contacts"
          class="flex items-center justify-center h-11 min-h-[2.75rem] bg-white text-blue-600 border border-blue-600 px-6 rounded-lg font-semibold hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition w-full sm:w-auto"
        >
          <svg
            class="w-5 h-5 mr-2 flex-shrink-0"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
            />
          </svg>
          Свържи се с нас
        </a>
      </div>
    </div>
  </div>
</Layout>
